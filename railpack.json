{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "python -m ml.train"
      ]
    }
  },
  "deploy": {
    "startCommand": "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
  }
}
